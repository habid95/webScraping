// si se llega a cambiar de calse o url solo se tiene que cambiar una sola vez y se puede reutilizar en otras app

const BASE_URL = "https://www.htfw.com/"

const name = ".product-item-link"

export default {
    BASE_URL,
    name
}